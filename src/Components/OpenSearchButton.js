import React from "react";

const OpenSearchButton = () => {
  return (
    <div className="open-search">
      <button onClick={() => alert("Hello")}>Add a book</button>
    </div>
  );
};

export default OpenSearchButton;
